export class Signin {
    //newClient.email, newClient.password, newClient.firstname, newClient.lastname, newClient.address
    constructor(public email:string,public password:string, public firstname:string, public lastname:string, public address:string) {}
}
